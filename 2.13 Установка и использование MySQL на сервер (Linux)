Регаемся на VPS (virtual private server)
Авторизуемся.

Проверим, есть ли mysql через командную строку, увидим, что нет.
Появятся рекомендаци, запустим первую из них, перезапустимся

Далее: apt install mysql server

Подключаемся к mysql, теперь работаем от его имени.

Создадим БД: CREATE DATABASE shop
Выведем список: SHOW DATABASES
Все появилось

Выйдем из командной строки mysql: quit;

Перейдем в папку: cd /home (где лежит дамп базы)
Посмотрим что там: ls -la (среди прочего shop.sql)? зальем его в базу
Войдем: mysql -u root -pPASSWORD (если была создана БД с паролем) либо
mysql shop < shop.sql

Подключимся к серверу: mysql
Выберем БД: USE shop;
Запросим список таблиц: SHOW TABLES;
Выведем все из таблицы: SELECT * FROM goods;
